# This app currently runs fully in the browser and does not require
# environment variables for Netlify deployment.
#
# Add browser-exposed Vite variables here only when needed:
# VITE_EXAMPLE_VALUE="example"
